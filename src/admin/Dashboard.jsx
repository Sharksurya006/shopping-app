import {useEffect,useState} from 'react';import {ResponsiveContainer,AreaChart,Area,XAxis,YAxis,Tooltip,CartesianGrid} from 'recharts';import {adminService} from '../services';import {inr} from '../utils';import {Skeleton} from '../components/ui';
export default function Dashboard(){const [s,setS]=useState(null),[r,setR]=useState([]);useEffect(()=>{document.title='Dashboard | NAYARA Admin';adminService.stats().then(setS);adminService.revenue().then(setR)},[]);
const cards=s&&[['Total revenue',inr(s.revenue)],['Orders',s.orders],['Customers',s.customers],['Low stock',s.lowStock]];
return <div><h1 className="text-xl font-semibold mb-6">Dashboard</h1><div className="grid grid-cols-2 lg:grid-cols-4 gap-4">{cards?cards.map(([k,v])=><div key={k} className="bg-bg border border-line p-4"><div className="text-xs text-mute">{k}</div><div className="text-2xl font-semibold mt-1">{v}</div></div>):Array.from({length:4},(_,i)=><Skeleton key={i} className="h-20"/>)}</div>
<div className="bg-bg border border-line p-4 mt-6 h-80"><div className="text-sm font-medium mb-2">Revenue</div><ResponsiveContainer width="100%" height="90%"><AreaChart data={r}><CartesianGrid stroke="rgb(var(--line))" vertical={false}/><XAxis dataKey="m" stroke="rgb(var(--mute))" fontSize={12}/><YAxis stroke="rgb(var(--mute))" fontSize={12}/><Tooltip contentStyle={{background:'rgb(var(--bg))',border:'1px solid rgb(var(--line))'}}/><Area dataKey="revenue" stroke="rgb(var(--ink))" fill="rgb(var(--hover))"/></AreaChart></ResponsiveContainer></div></div>}
